// Source code is decompiled from a .class file using FernFlower decompiler.
package com.java.www;

public class PrimeNumbers1to100 {
   public PrimeNumbers1to100() {
   }

   void method() {
      for(int n = 2; n <= 100; ++n) {
         boolean flag = true;

         for(int i = 2; i <= n / 2; ++i) {
            if (n % i == 0) {
               flag = false;
               break;
            }
         }

         if (flag) {
            System.out.print(n + ",");
         }
      }

   }

   public static void main(String[] args) {
      PrimeNumbers1to100 obj = new PrimeNumbers1to100();
      obj.method();
   }
}
